// Pusher/Echo disabled — using polling + WebPush instead

let echo = null;

export function connectEcho(token) {
  // Pusher disabled — relying on notification polling (pwa.js) and WebPush
}

export function listenForNotifications(userId) {
  // Pusher disabled
}

export function disconnectEcho() {
  // Pusher disabled
}
