You are working on the existing SmartMall repository imported from GitHub.

IMPORTANT:
Do NOT modify, delete, rename, refactor, or break any existing production code.
Do NOT change the existing React/Vite website.
Do NOT change the Laravel backend or database.
Do NOT create or modify database migrations.
Do NOT remove any existing functionality.

My goal is to build a completely independent professional mobile application for SmartMall from scratch using:

- React Native
- Expo
- TypeScript

The mobile app must use the existing Laravel API as its backend.

For this first task, DO NOT build the mobile app yet.

Instead, perform a complete read-only audit of the existing repository.

Analyze and report:

1. Current project structure.
2. Frontend technology and architecture.
3. Laravel backend structure.
4. API base URL and API route structure.
5. All existing API endpoints relevant to:
   - authentication
   - customers
   - agents
   - products
   - product categories
   - goods
   - goods categories
   - home sections
   - cards
   - cart
   - orders
   - user profile
   - notifications
   - QR/barcode features
6. Authentication mechanism and token/session handling.
7. User roles and permissions.
8. Existing image/storage services such as Cloudinary.
9. Existing push notification implementation.
10. Existing QR/barcode implementation.
11. Existing frontend pages and user flows.
12. Existing mobile/PWA/Capacitor-related code, if any.
13. Important performance considerations, especially the large product catalog.
14. APIs that can be reused directly by React Native.
15. APIs that may need improvement before the mobile app.
16. Any security issues that could affect a mobile client.
17. Any environment variables or secrets that must NOT be exposed to the mobile application.

Then propose a recommended React Native + Expo architecture for a NEW independent mobile app.

Do not create files yet.
Do not install packages yet.
Do not modify existing files yet.

At the end, give me:
A) Repository audit
B) Existing API map
C) Authentication/API strategy
D) Recommended mobile architecture
E) Proposed mobile folder structure
F) Step-by-step implementation plan

WAIT for my approval before making any changes.